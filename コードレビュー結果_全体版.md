# career-sugoroku 全体コードレビュー結果（拡張版）

対象: origin/main（コミット `177931c`）
すべて実際のソースコードで再現性を確認した内容のみを記載しています。

---

## 1. ルール説明と実装の乖離

### 1-1. ダイスの出目が「1〜6」と説明されているが、実装は「1〜2」しかない

`src/components/GuideContent.tsx` 76行目（ルールガイドのSTEP解説）

```tsx
{ step: '01', title: 'ダイスを振る', icon: '🎲', desc: '1〜6の出目でマスを進みます。偶発的なイベントに遭遇！' },
```

一方、実際のダイス実装 `src/components/DiceRoller.tsx` 19・23行目は

```ts
setLastValue(Math.floor(Math.random() * 2) + 1);   // ← 1か2しか出ない
```

で、コンポーネント自身のUI文言も「移動ダイス (出目 1 〜 2)」と正しく1〜2を表示しています。つまり**ルールガイドの説明文だけが実態と食い違っている**状態です（見た目は普通のすごろくを想像させますが、実際は1〜2しか動かない小刻み進行です）。

### 1-2. 「余暇(Leisure)の山札」は4つ目の山札として説明されているが、実装上は完全に到達不能

`src/components/GuideContent.tsx` 252〜270行目で「🌴 ライフ / 余暇 (Leisure) の山札（計10枚）」として、Labor/Learn/Love山札と並ぶ4つ目の独立した山札があるように説明しています。

しかし `src/data/decks.ts` を見ると、該当する `LEISURE_DECK`（s1〜s10）は定義されているものの、

```ts
export const LIFE_DECK = [...LOVE_DECK];   // ← LEISURE_DECKではなくLOVE_DECKが使われている
```

`src/types/game.ts` の `DeckType` にも `'leisure'` という値自体が存在せず（`'work' | 'learn' | 'life' | 'any'` のみ）、`gameEngine.ts` の `getDeckCards()` にも `LEISURE_DECK` を参照する分岐がありません。「贅沢有給旅行」で"Leisure山からドロー"すると案内される箇所（`GameContainer.tsx`）も、実際には `targetDeck = 'life'`（= LOVE_DECKの中身）を引いています。
つまり**「Leisure専用の山札」は定義されているだけで、ゲーム中に一度も引かれることのない完全なデッドコード**です。ガイドの「山札は4種類」という説明も実質3種類が正しい実装内容です。

### 1-3. 「プロジェクト達成で勝利ptを獲得」という説明に対応する実装がない

`src/components/GuideContent.tsx` 80行目

```tsx
{ step: '05', title: 'プロジェクト達成', icon: '🎯', desc: '必要スキルを満たすプロジェクトを完了し勝利ptを獲得！' }
```

とありますが、`gameEngine.ts` / `GameContainer.tsx` のどこにも「勝利pt」という得点・スコアリングの仕組みは存在しません。協力プロジェクト達成で得られるのは4Lステータス・資金・体力のみで、勝敗を決める独立した「pt」は実装されていません。

### 1-4.（前回報告の再確認）カード選択メカニクスが機能していない

`src/components/CardSelectionModal.tsx` 19行目 `Math.max(selectCount, drawnCards.length)` が常に `drawnCards.length` になるため、30代「3枚から任意2枚選択」・40〜50代「2枚から任意1枚選択・効果2倍」は実際には選択UIがなく、引いたカード全部がそのまま加算されます。40〜50代は結果的に「1枚を2倍」ではなく「2枚をそれぞれ2倍」を得ており、想定より大きく強くなっています。

### 1-5.（前回報告の再確認）`tile.skillPt` が実質デッドコードで、盤面表示と実態が食い違う

`GameContainer.tsx` 内で `tile.skillPt` は必ず `gainedLearn`（実際に引いたLearnカード量）で上書きされ、`gainedLearn === 0` の場合はスキル配分フェーズ自体に進みません。一方 `Board.tsx` 140行目は盤面カードに `+{tile.skillPt} Skill pt` と表示しており、プレイヤーには「このマスに乗れば固定pt数がもらえる」ように見えますが、実際は0ptで終わることもあります。

### 1-6.（前回報告の補足）キャラクター固有パッシブ3種の未実装

`GuideContent.tsx` 103〜105行目でイオリ「受講費50%割引」・アオイ「体力回復1.5倍」・ソウタ「愛で体力+10回復」と明記されていますが、`GameContainer.tsx` にはこの3つを反映するロジックが存在しません（これは前回すでに実装パッチを作成済みですが、まだ取り込まれていません）。

---

## 2. コードの保守性・重複

- **ヘッダーが2種類、別々に実装されている**: `src/components/HeaderNav.tsx`（`/rules` `/theory` `/guide` ページ用）と、`GameContainer.tsx` 内に直接書かれたヘッダー（トップページ用）は見た目・ボタン構成が別物で、共通化されていません。今後デザイン変更する際に片方だけ直して食い違う懸念があります。
- **`/guide` ページが `/rules` ページと実質同一内容**: `src/pages/guide.astro` は `initialTab="rules"` / `activeTab="rules"` を固定で渡しており、`/rules` ページと表示内容・ナビゲーションのハイライトが区別できません。ページタイトルとmeta descriptionだけが異なる状態で、存在意義が薄い重複ページになっています。

## 3. UI細部

- `src/components/StatusPanel.tsx` のスキルバーは `maxVal = 10` で固定されていますが、自己決定目標「自律スペシャリスト型」は `targetSkillsSum: 14`（4スキル合計）を要求するため、終盤は個別スキル値が10を超えてバーが天井に張り付いたまま伸びが見えなくなる可能性があります。

## 4. バランス調整の検討候補（数値未検証・要シミュレーション）

- 自己決定目標4種の難易度差（特に「自律スペシャリスト型」の資金50CR・スキル合計14ptは他より明確に重い）
- 6キャラクターの相対的な強さ（特にチヒロの毎ターン発動 vs エイジのプロジェクト達成時のみ発動、という発動頻度の差）
- 協力プロジェクト3種の報酬とコストのバランス
- 有給休暇の「最低1回消化」は罰則が称号表示（ワーカホリック表記）のみで、ゲームプレイ上の強制力は弱い

---

以上、原則としてコード上で再現性を確認できたものを優先度順に記載しています。4章のみ数値シミュレーションが必要な「要検証」の項目です。
