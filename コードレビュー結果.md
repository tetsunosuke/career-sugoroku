# career-sugoroku コードレビュー結果

対象: origin/main（コミット `177931c`、レビュー時点の最新）
直接コード上で再現性を確認した内容のみを記載しています。

## 検証済み（このまま該当）

### 1. カード選択メカニクスが機能していない

`src/components/CardSelectionModal.tsx` 19行目

```ts
const activeCards = drawnCards.slice(0, Math.min(drawnCards.length, Math.max(selectCount, drawnCards.length)));
```

`Math.max(selectCount, drawnCards.length)` は `selectCount ≦ drawnCards.length` である限り常に `drawnCards.length` になるため、この式は事実上「引いたカードを全部採用する」としか動作しません。画面上にも「どのカードを残すか」を選ぶUIはなく、合計値表示と確定ボタンのみです。origin/mainの現物で確認済みで、未修正のままです。

- 30代「3枚引いて任意の2枚を選択」→ 実際は3枚とも加算
- 40〜50代「2枚引いて任意の1枚を選択・効果2倍」→ 実際は2枚とも2倍で加算（意図の実質4倍相当）

### 2. `tile.skillPt` が実質デッドコード

`src/components/GameContainer.tsx` 333〜336行目

```ts
if (gainedLearn > 0) {
  setPendingSkillBasePt(gainedLearn);   // ← ここで tile.skillPt を必ず上書き
  setPhase('SKILL_ALLOCATION');
} else {
  // gainedLearn === 0 のときは SKILL_ALLOCATION 自体に進まない
}
```

`tile.skillPt` は一度セットされても必ず `gainedLearn`（実際に引いたLearnカードの量）で上書きされ、かつ `gainedLearn === 0` のときはスキル配分フェーズ自体に進みません。さらに `src/components/Board.tsx` 140行目で盤面に `+{tile.skillPt} Skill pt` と表示しているため、プレイヤーには「このマスに乗れば決まったpt数のスキルが手に入る」ように見えるが、実際は引いたカード次第で0ptのこともある、という表示と実態の食い違いも確認できました。マス#10「部署異動」の"柔軟スキル強制"も、workデッキからLearnカードを引けた場合にしか発動しません。

## 結論

上記2項目は、いずれも思い込みではなく origin/main の現物コードで再現性を確認済みです。修正するかどうか、するとしたらどちらの方向（選択UIをきちんと実装する／それとも「全部もらえる」を仕様として説明文の方を書き換える）で揃えるかは設計判断になるので、方針が決まればそのまま実装まで対応できます。
